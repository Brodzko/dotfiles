# Log view: press D to preview delta output of commit
bind main D !>sh -c "git show --color=always %(commit)"

# Diff view: same behavior for current commit
bind diff D !sh -c "git show --color=always %(commit)"

# Status view: show working tree delta diff
bind status D !sh -c "git diff --color"